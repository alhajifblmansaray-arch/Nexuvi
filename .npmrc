strict-peer-dependencies=false
auto-install-peers=true
# Strict node_modules layout: a package can only import what it declares.
# This is what enforces the module-boundary rule in blueprint §10.2 at install time.
node-linker=isolated
